@ListItem { Comandi "p" e "z" non in history }
@ListItem { Se molla una eccezione in salvataggio resetta comunque il flag modificato }
@ListItem { Controllare in comando "w" che non si generino buffer con lo stesso nome }
